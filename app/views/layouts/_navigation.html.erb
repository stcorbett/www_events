<nav class="navbar navbar-fixed-top header">
  <div class="col-md-12">
    <div class="navbar-header">
      <strong>
        <%= link_to "Lakes of Fire #{t(:year)} #{t(:event_name).upcase} What Where When Guide", root_path, class: 'navbar-brand' %>
      </strong>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <div class="navbar-collapse collapse" id="navbar-collapse" style="height: 1px;">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li>
            <%= link_to "#{admin_badge(current_user)} #{current_user.name} ".html_safe, "#_", class: "name" %>
          </li>
          <li>
            <%= link_to "Camps", derived_locations_path %>
          </li>
        <% end %>

        <li>
          <%= link_to "Privacy", privacy_path %>
        </li>

        <% if current_user && current_user.admin? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle admin-dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              Admin <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li class="admin-nav-item">
                <%= link_to "Files", hosted_files_path %>
              </li>
              <li class="admin-nav-item">
                <%= link_to "Locations", admin_locations_path %>
              </li>
              <li class="admin-nav-item">
                <%= link_to "Camps", admin_camps_path %>
              </li>
            </ul>
          </li>
        <% end %>

        <% if current_user %>
          <li>
            <%= link_to "Logout", logout_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
