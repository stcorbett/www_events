<nav class="navbar navbar-fixed-top header">
  <div class="col-md-12">
    <div class="navbar-header">
      <strong>
        <%= link_to "Lakes of Fire #{t(:year)} #{t(:event_name).upcase} What Where When Guide", root_path, class: 'navbar-brand' %>
      </strong>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <div class="navbar-collapse collapse" id="navbar-collapse" style="height: 1px;">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li>
            <%= link_to "#{admin_badge(current_user)} #{current_user.name} ".html_safe, "#_", class: "name" %>
          </li>

          <li>
            <%= link_to "Camps", locations_path %>
          </li>
        <% end %>

        <li>
          <%= link_to "Privacy", privacy_path %>
        </li>

        <% if current_user %>
          <li>
            <%= link_to "Logout", logout_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
