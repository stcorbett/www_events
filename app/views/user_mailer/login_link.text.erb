Hi<%= " #{@user.name}" if @user.name.present? %>,

Click the link below to sign in to the WhatWhereWhen event guide:

<%= @url %>

This link is good for one hour. If you didn't request it, you can ignore this email.

— Lakes of Fire Info_Bot
